<!-- project: github.com/Monard2033/RaspberryPicoUSBHost | observed: 2026-09-10 | status: active -->
- OTA root causes fixed today: (1) `watchdog_update()` lives in XIP flash — calling it between slot-0 erase/program caused XIP fetch on erased flash → wedge → watchdog reset → BOOTSEL entry. Fix: `watchdog_disable()` before swap, no watchdog calls inside. (2) `dfu_swap_sram_buffer` raised 64→128 KB (SRAM verified 0x20005074+0x20000 < 0x20040000).
- OTA protocol v2 (2026-09-10): DFU_DATA chunk = 5 payload bytes + seq byte in data[7]; RP2040 accepts only exact next seq (mod 256), drops duplicates/out-of-order idempotently; PC chunks paced 2.4 ms (~2 KB/s stable limit, 5 KB/s corrupts); recovery re-send only after 2 s frozen device count, re-sent chunks carry correct seq (derived from offset). Radio transfers corrupt when user types simultaneously — tested and confirmed.
- One-shot migration tool `tools/ota_bridge_v1_to_v2.py` installed v2 firmware over v1 device; v2 is now on the RP2040 (CRC 0x4F43F4F7). Old exe/GUI had false CRC verification ("Potrivire 100%" without comparing) — rebuilt from source.
- Sonix NumLock LED physical sync = BIG TODO (task_751c8d37): SET_REPORT on EP0 poisons SN32 EP1 (≥4-key freeze) regardless of timing (2–4 s gap still freezes); EP1 toggle reset + idle-gate did not help. Windows lock state already reaches RP2040 via radio reverse-ACK (spi_process_ack). User decided: keep it out of the firmware for now.
- Sonix SN32 lesson: keyboard HID LED output is standard Usage Page (LEDs) in the keyboard interface (not Consumer); local keyboard_led_state in HEAD is write-only/dangling; SN32 self-manages its LED at power-up/toggle.
- GUI Flash_Ota.exe (PyQt6): no auto-preselected .wkota path; i18n EN/RU/RO default EN; window 680×760; status bar capped 40px wrap, event log Expanding (absorbs all extra height). Build via tools/build_ota_flasher.ps1.
- RP2040 flash procedure that works on this machine: picotool blocked by missing WinUSB driver ("RP2 Boot"); fallback = copy UF2 to RPI-RP2 mass-storage drive (worked every time).
- tools/ cleanup done: kept flash_ota.py, flash_ota_cmd.py+exe, flash_ota_gui.py+Flash_Ota.exe, probe_ota_link, make_ota_package, ota_bridge_v1_to_v2, build_ota_flasher.ps1, save_good_build, flash_status_diag; stale build/backup/spec/dist/venv dirs removed.
- Receiver+Transmitter (nRF, NCS projects C:\ncs\v3.4.0\myproject\) carry DFU dedup bypass for frames 0x10–0x1F; user reflashed both modules himself.
<!-- /openclaw:memory -->
# 2026-09-10

<!-- project: github.com/Monard2033/RaspberryPicoUSBHost | observed: 2026-09-10 | status: active -->
- OTA root causes fixed: (1) `watchdog_update()` lives in XIP flash — calling it between slot-0 erase/program caused XIP fetch on erased flash → wedge → watchdog reset → BOOTSEL entry. Fix: `watchdog_disable()` before swap, no watchdog calls inside. (2) `dfu_swap_sram_buffer` raised 64→128 KB (SRAM verified 0x20005074+0x20000 < 0x20040000).
- OTA protocol v2: DFU_DATA chunk = 5 payload bytes + seq byte in data[7]; RP2040 accepts only exact next seq (mod 256), drops duplicates/out-of-order idempotently; PC chunks paced 2.4 ms (~2 KB/s stable limit, 5 KB/s corrupts); recovery re-send only after 2 s frozen device count, re-sent chunks carry correct seq (derived from byte offset). Radio transfers corrupt when user types simultaneously.
- One-shot migration tool `tools/ota_bridge_v1_to_v2.py` installed v2 firmware over v1 device; v2 is on the RP2040 (CRC 0x4F43F4F7). Old exe/GUI had false CRC verification ("Potrivire 100%" without comparing) — rebuilt from source.
- Sonix NumLock LED physical sync = BIG TODO (task_751c8d37): SET_REPORT on EP0 poisons SN32 EP1 (≥4-key freeze) regardless of timing (2–4 s gap still freezes); EP1 toggle reset + idle-gate did not help. Windows lock state already reaches RP2040 via radio reverse-ACK (spi_process_ack). User decision: keep LED sync out of the firmware for now.
- Sonix SN32 lesson: keyboard HID LED output is standard Usage Page (LEDs) in the keyboard interface (not Consumer); local keyboard_led_state in HEAD is write-only/dangling; SN32 self-manages its LED at power-up/toggle.
- GUI Flash_Ota.exe (PyQt6): no auto-preselected .wkota path; i18n EN/RU/RO default EN; window 680×760; status bar capped 40px wrap, event log Expanding (absorbs all extra height). Build via tools/build_ota_flasher.ps1.
- RP2040 flash procedure that works on this machine: picotool blocked by missing WinUSB driver ("RP2 Boot"); fallback = copy UF2 to RPI-RP2 mass-storage drive (worked every time).
- tools/ cleanup done: kept flash_ota.py, flash_ota_cmd.py+exe, flash_ota_gui.py+Flash_Ota.exe, probe_ota_link, make_ota_package, ota_bridge_v1_to_v2, build_ota_flasher.ps1, save_good_build, flash_status_diag; stale build/backup/spec/dist/venv dirs removed.
- Receiver+Transmitter (nRF, NCS projects C:\ncs\v3.4.0\myproject\) carry DFU dedup bypass for frames 0x10–0x1F; user reflashed both modules himself.
- Pending request (20:35): sweep the whole WirelessKeyboard dir, add all important project files, local commit + PR to origin/feature/wireless-ota-dfu, treating local files as authoritative over remote.
